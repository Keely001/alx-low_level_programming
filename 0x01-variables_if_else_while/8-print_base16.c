#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Base 16 numbers
 * Return: Always 0
 */
int main(void)
{
	int a;
	char b
	
	for (a = '0'; a <= '9'; a++)
	putchar(a);

	for (b = 'a'; b <= 'f'; b++)
	putchar(b);
	putchar('\n');

	return (0);
}
