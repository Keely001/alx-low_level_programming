#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Printing integers with single digits
 * Return: Always 0
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
	printf("%d", a);
	printf("\n");

	return (0);
}
